flask>=3.1.0
python-dotenv>=1.0.1
werkzeug>=3.1.3
openpyxl>=3.1.5
schedule>=1.2.2
waitress>=3.0.0        # 운영 WSGI 서버 (serve.py) — Flask 개발서버 대체
korean-lunar-calendar>=0.3.1   # 할 일 달력 음력 표기
holidays>=0.60                 # 할 일 달력 법정 공휴일(대체공휴일 포함)
# 마이그레이션 fuzzy 매칭은 표준 라이브러리 difflib 사용 (rapidfuzz 미사용)
