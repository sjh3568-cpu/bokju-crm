# 전 화면 디자인 점검 — 한 줄·정렬·문구·크기·형식 (2026-09-21)

> 이 파일 하나가 작업의 전부입니다. 에이전트들은 말로 인계하지 않고 이 파일을
> 읽고 자기 절만 채웁니다. 자기 절이 아닌 곳은 읽기만 합니다.
> 해당 없는 절은 지우지 말고 `해당없음` 이라고 적습니다.
> **환자 이름·연락처는 적지 않는다** (행 번호·"n번째 행"으로만 지칭).

---

## ① 무엇을  — `plan`

**배경**
사용자가 대시보드 '오늘 처리 필요' 섹션에서 **미처리 문의 행의 높이가 옆 행들과 다른 것**을 발견했다
(사용자 요청 2026-09-21). 이런 종류의 불일치를 **사이드바의 모든 화면**에서 찾아 달라는 요청.
점검 항목: 한 줄 유지(줄바꿈·잘림·가로 스크롤) · 정렬(같은 행 높이, 숫자 우측, 세로 중앙, 간격) ·
문구(같은 뜻 다른 이름, 띄어쓰기, 단위) · 크기(제목·KPI·배지·버튼) · 형식(날짜·시간·숫자·빈값).

**하려는 것**
- 개발 서버 화면 31장을 **1440×850 실제 브라우저(크로미움)** 로 찍어 눈으로 보고, DOM 치수로 뒷받침한다.
- 결함마다 **원인 셀렉터(파일:줄)** 까지 밝힌다. 사용자가 든 예(미처리 행 높이)는 반드시 원인 규명.

**안 할 것**
- 파일 수정 (템플릿·CSS·파이썬 모두 읽기만). 이 작업 파일 하나만 쓴다.
- 백엔드·데이터 수정. 수치 불일치는 보고만.

**할 일**
- [x] 31개 경로 촬영(뷰포트 + full_page) + 가로 스크롤·잘림·형제 높이 자동 검출
- [x] 스크린샷을 직접 열어 확인
- [x] 결함별 원인 셀렉터 확인
- [x] ③ 절에 A/B/C/D 보고

**사용자 결정 (2026-09-21)** — 점검 결과 중 세 묶음을 **수정**한다. 템플릿·CSS·파이썬 수정 허용, 커밋은 하지 않음.
데이터 로직(회복기 비율 불일치 등 B-26)은 범위 밖. 묶음마다 `pytest -q` 통과 확인.
- 묶음 1: 대시보드 '오늘 처리 필요' 표 + `.nowrap`·`.filter-bar .date-range` 전역화 + 넘치는 표 4곳(상담목록·모병원·주간 현황·기관협력). 열을 숨기지 않고 1440 접힘 폭 안에.
- 묶음 2: 제목 상단 패딩·h1 크기 통일, 상단바 버튼 높이 1종, `.form-draft-state` 이중 정의, 기관협력 autofocus, KPI 카드 점검(한 화면 안 불일치·잘림만 수정).
- 묶음 3: 날짜 표기는 화면 글자 한정 `2026-09-21(월)` / 축약 `09-21(월)` / 기간 `2026-09-01 ~ 2026-09-21`(input·API·CSV·DB 형식은 불변), 띄어쓰기는 다수 쪽, 빈값 `-`, 1,000 이상 콤마, 호실은 다수 쪽, 퍼센트 1자리.
  상담자/상담사 · 입원경로/유입경로 · To-Do 이름 · 새 상담/새 상담일지는 **결정 보류 — 추천안만**.

**확인 필요**
- `.env`의 `APP_PASSWORD`가 **어떤 계정의 현재 비밀번호와도 맞지 않는다**(DB 7계정 해시 전부 불일치 —
  어드민 비번이 화면에서 바뀐 듯). 브라우저 로그인 대신 tests와 같은 방식(Flask `test_client` +
  `session_transaction`, 메모리 세션)으로 페이지를 인프로세스 렌더링하고 Playwright `page.route`로 물려 찍었다.
  GET만 통과시켰고 쓰기 요청은 403으로 막았다. 감사 로그에 어드민 계정의 '상담 조회 #8070·환자 조회 #7830·
  기관협력 조회'가 이 점검분으로 남아 있다(2026-09-21 00:50~00:51).
- 캡처 환경은 외부 CDN을 막았다 → Pretendard 대신 맑은 고딕, `report_monthly.html:4`의 Chart.js도 안 떠서
  월간보고서 차트는 빈칸으로 찍혔다. **사내망에서 CDN이 막히면 사용자 PC도 같은 모습**이다(CLAUDE.md 폰트 메모와 같은 위험).
  글자 폭 차이로 ±수 px 경계 사례는 '경계'라고 표시했고, 수십~수백 px 넘치는 건은 폰트와 무관하다.

---

## ② 어떻게 만들었나  — 본인

> 2026-09-21 수정 작업 진행 기록. 중단돼도 여기서 이어받는다. 커밋 안 함.
> pytest 기준선: 370 passed / 1 failed — `tests/test_away_as_discharge.py::test_week_out_counts_the_departure`는
> **변경 전(git stash)에도 실패**하는 백엔드 기존 실패(4e168f3 관련). 이 작업과 무관, 보고만.

### 묶음 1 — 완료
- `.nowrap` → `style.css` 전역(:212), 개별 템플릿 중복 정의 제거(audit/consult_list/inquiries/stats_staff/dashboard).
- `.filter-bar .date-range`·`.dr-sep` → `style.css`(:651~) 전역. audit.html 기간 입력이 가로로 붙음.
- 대시보드 '오늘 처리 필요'(`.aq-table`): `dashboard.html` colgroup(구분68·환자72·주상병66·내용=나머지·경과100·액션118) + `table-layout:fixed`,
  액션 셀은 대표 버튼 1개 + `<details class="aq-more">` 더보기. `.tbl-wrap:has(> .aq-table){overflow:visible}`로 마지막 행 메뉴가 안 잘리게.
  `app._dashboard_elapsed_label` 문구에서 ' 경과' 접미 제거(열 머리글이 '경과'라 중복, 100px 셀에서 "1일 19…" 잘림). 템플릿 사용처 없음(views/main.py elapsed_label은 템플릿 미사용).
  결과: 문의 행 48px / 운행 행 47px(±1은 배지), 6열 다 보임, 더보기 메뉴는 셀 아래로 열림(after-aq-open.png).
- 상담목록 `consult_list.html`: `.tbl-compact{min-width:1300px}`(주석과 일치), ≤1500 규칙 성별·나이 32 / 상담방법 56 / 회복기대상 66 / 상담결과 68 / 입원진행 84 / 만료일 98 / 상담자 46.
  정렬 안 된 열의 ⇅는 ≤1500에서 hover 때만(`th.sortable:not(.sorted):not(:hover) .sort-ind{display:none}`). 표 1346 = 카드 1346, 머리글 잘림 0.
- 모병원 분석 `stats_hospitals.html`: `@media(max-width:1500px)` 블록 추가 — min-width 해제, 패딩·막대·배지 축소, 병원명·주요 질환 말줄임. `td.num{white-space:nowrap}`(“16 명” 꺾임).
  결과: 표 1154 = 카드 1154(사이드바 펼침에서도 16열 전부).
- 주간 현황 `report_weekly.html`: ≤1500에서 min-width 해제, 10px, 날짜 열 56px, "검색(블로그)"는 `<wbr>`로 괄호 앞 줄바꿈. PNG 저장은 1560px로 따로 그려 영향 없음. 표 1090 = 카드 1090.
- 기관협력 `partners.css`: ≤1500에서 min-width 해제 + 열 % 재배분(16/11/14), `.coop-perf-cell`은 `overflow-wrap:normal; word-break:keep-all`, 실적 문구는 `<span class="nowrap">`, '방문 필요' 태그는 block.
  `partners.html:13` autofocus 제거(진입 시 871px 스크롤 → 0).

### 묶음 2 — 완료
- 제목 위 여백: `style.css:136` 아래 `main.container{padding-top:26px;padding-bottom:34px}` 추가(특이성 0,1,1 > `.container`).
  `<main class="container">`는 base.html 한 곳(partner_embed는 plain `<main>` + `.coop-embedded main`). 덮어쓰던 두 곳도 정리 —
  `consult_list.html:454` `body .container{padding:0 12px}` → 좌우만, `dashboard.css:15` `.page-dashboard main{padding-top:14px}` 제거.
  결과(h1probe): h1 top 59→85 전 화면 동일(문서·모병원·직원소개는 위에 뒤로가기/눈썹 줄이 있어 110 — 구조상 정당).
- h1 크기 한 값 23.68px(`.page-header h1` 1.48em): `stats.html:211` 1.2em 제거, `stats_hospitals.html:19` 1.6rem 제거, `dashboard.css:48` 1.3rem→1.48em/820.
  todos·users·audit·admin_import의 `<h2>`→`<h1>`(h2에 걸린 CSS 없음 확인, `.page-header h2` 규칙은 이제 미사용이지만 남겨둠).
- 제목 줄 버튼 1종: `.page-header .btn, .dash-head .btn{height:36px; padding:0 14px!important; font-size:.88em}`(style.css). 24화면 전부 36px(h1probe).
- 상단바 3요소 36px: `.topbar .global-search form` 38→36, `.header-tool-btn` 36×36, `.header-new-consult` height 36, `.command-status` 칩 inline-flex 36(gap .28em로 공백 유지).
- `.form-draft-state`: style.css 하나로(`position:static`, 폼 흐름 안 우측). consult_form.html:1685의 두 번째 정의 제거. 스크린샷 after-03-consult-new-s0.
- 기관협력 autofocus 제거(묶음 1에서 처리).
- KPI 카드: 대시보드 `.kc{align-content:start}`(같은 줄 카드의 숫자·배지 top 82/74 → 전부 74), 보조줄 문구 축약(지난달 동기간→지난달·빈 병상 82/355·포화 2병동·10병동·회복기 접두 삭제) → `…` 잘림 0.
  통계 `.kpi-row .stat-card` justify-content center→flex-start(1~3번/4~6번 기준선 15px 어긋남 해소).
  재원 KPI 라벨 축약: '회복기 S005 · 비회복기 S006'→'회복기 · 비회복기'(title에 코드), '📣 회복기 종료·퇴원 안내 D-30'→'📣 종료·퇴원 안내 D-30',
  추이 탭 '기간 평균 연인원 기준 · 단순평균'→'기간 평균 단순평균 n%'(title에 설명), '최근 12개월 월말 평균 달마다…'→ 보조문 title로. 잘림 0.
  화면 간 스킨 차이(숫자 28/25.6/22.4px 등)는 ③ 표로 보고만.

### 묶음 3 — 완료 (형식·문구)
- **날짜 표기** (화면 글자만, input value·API·CSV·DB 불변):
  - 필터(app.py:643~): `krdate` `2026-09-21`(그대로) · `krdate_wd` `9/21(월)`→`09-21(월)`(축약형) · `krdate_wd_full` `2026-09-21(월)`(그대로) ·
    `krdate_short` `26.09.03`→`2026-09-03` · `krdate_short_wd` `26.09.03(목)`→`2026-09-03(목)`. 규칙 주석을 `krdate_wd` 위에 적어 둠.
  - 수기 포맷: 상단바 `today_header`(app.py:540) `2026.09.21 (월)`→`2026-09-21(월)` · 대시보드 입퇴원 안내(main.py:381) `2026.09.20(일)`→`2026-09-20(일)` ·
    대시보드 KPI 안내줄 `09.01~09.21`→`09-01 ~ 09-21` · 입퇴원 표 일시 `(krdate_short_wd)[3:]`→`krdate_wd`(`09-21(월)`) ·
    주간표 날짜 `%m/%d`→`%m-%d`(models.py:4357), 주간 기간 라벨 `%Y.%m.%d`→`%Y-%m-%d`(:4424) · 회복기 추이 축 라벨 `%m.%d`→`%m-%d`(views/ward.py 3곳, dashboard_metrics.py:121) ·
    ward.html 병상 예약 목록 JS `.replace('-', '.')` 제거. 달력 패널 제목 `9월 21일 (일)`은 문장형이라 그대로.
  - 폭 보정: 상담목록 ≤1500 상담일 80→100(실측 97) · 발병일 60→64 · 입원완료일/퇴원완료일 70→74 · 만료일 98 · 회복기대상 66 · 입원진행 76 · 연락처 134→130
    (>1800 규칙 상담일 94→108, 발병일 74→82). `td.onset-date-cell`·`.admexp-row > span`을 nowrap으로 — `2026-08-31`이 하이픈에서 두 줄로 꺾이던 것.
    대시보드 입퇴원 표 ≤1500 c-when 74→80 · c-onset 66→74 · c-room 54→60 (c-hosp 130→124, c-sev 132→126에서 회수). 잘림 0(clip 검사).
- **띄어쓰기** (다수 쪽):

  | 항목 | 사용 횟수(템플릿·views·app) | 채택 | 바꾼 곳 |
  |---|---|---|---|
  | 입원예정 / 입원 예정 | 170 / 52 | **입원예정** | 상태·라벨·머리글 14곳(dashboard·ward·consult_detail·_ward_roster·app.py). 문장 속("입원 예정 환자가 오면…" 알림·SMS 본문·주석)은 그대로 |
  | 상담목록 / 상담 목록 | 27 / 13 | **상담목록** | consult_list 제목·title, help.html 7곳 |
  | 처리완료 / 처리 완료 | 16 / 11 | **처리완료** | 대시보드 액션 버튼 '완료'→'처리완료'(2), document_detail 버튼, views/documents.py STATUS_LABELS. flash 문장은 그대로 |
- **빈값**: `—`(41곳: audit·users·_ward_moves·report_monthly·_ward_roster·documents·stats_staff·consult_list JS·consult_form·stats_hospitals) · `(미기재)`(dashboard:303) · 공란(patient_detail 상담 이력 4열) → `-`.
  보호자: 이름·관계·연락처 중 있는 조각만 붙이고 셋 다 없을 때만 `-`(patient_detail·consult_detail·ward 3곳·dashboard). "- (아들) 010-…", "- · 010-…" 해소.
- **숫자 콤마**: `comma` 필터 신설(app.py, pct1 위). 상담목록 총 건수 2곳·기관협력 마스터 곳수 3곳·audit.html의 인라인 `'{:,}'.format` 3곳을 필터로.
- **호실**: `room_no` 필터('NNN호')를 _ward_moves·_ward_roster·consult_detail·ward.html 2곳·patient_detail에 적용 → "317호 ★"/"505"/"1005호" → 모두 'NNN호'.
- **퍼센트**: `pct2`(둘째자리) → **`pct1`**(첫째자리)로 이름·구현 변경, 템플릿 27곳 치환. 41.39%→41.4%, 0.00%→0.0%. 40% 판정은 값으로 하므로 표기와 무관(도크스트링에 적음).
- 외진 배지 '복귀 18 명' → '복귀 18명'(ward.html `.away-badge` gap 0 + `b{margin-left:4px}`).
- 테스트 4개는 옛 표기 문자열 assert만 새 표기로 고침: test_consultation_display('26.01.02'→'>2026-01-02</td>'), test_dashboard_strip(%.2f→%.1f), test_partnerships(상단바 날짜 정규식), test_ward_moves('>502</td>'→'>502호</td>'). 로직 테스트는 손대지 않음.
- **결정 보류 4건 — 추천안**(고치지 않음):
  1. 상담자 23 / 상담사 20(템플릿) — DB 컬럼 `consultations.counselor`, config.py 주석 "상담자 — 현 직원", 종이 상담일지 칸 이름 '상담자'. **1안: 화면 필드 라벨은 '상담자', 직군·계정 역할(ROLE_LABELS staff='상담사')만 '상담사'.**
  2. 입원경로 5 / 유입경로 25 — 폼 fieldset(consult_form:952)·상세(consult_detail:405)·인쇄·통계 h3 '입원경로별 성과'만 '입원경로'. 데이터는 `referral_source_type`(유입경로)이고 `admission_route`는 별도 컬럼이라 '입원경로'는 이름이 틀리기까지 함. **1안: 5곳을 '유입경로'로.**
  3. To-Do 이름 — To-DO List 2(사이드바 FAB·help) / 내 할 일 11 / 오늘 할 일 1 / 나의 ToDo 3 / 새 ToDo 2 / '할 일' 38. **1안: 한글 '할 일'로 — 메뉴 '할 일', 페이지 '내 할 일', 버튼 '＋ 새 할 일', 달력 필터 '할 일'.** 'To-DO'는 표기 자체가 오타형(DO 대문자).
  4. 새 상담 17 / 새 상담일지 6 — 사이드바·상단바 '새 상담'(짧은 칸), 페이지 제목·본문 버튼 '새 상담일지'. 종이 양식 이름이 '상담일지'. **1안: 좁은 곳(사이드바·상단바 칩)은 '새 상담' 유지, 버튼·제목은 '새 상담일지' — 즉 현 상태 유지를 결정으로 확정.** 하나로 하려면 '새 상담일지'.

### 남긴 것(범위 밖·보고만)
- pytest 기존 실패 1건(`test_away_as_discharge::test_week_out_counts_the_departure`) — 변경 전에도 실패, 백엔드(4e168f3 외진 복귀) 확인 필요.
- B-25 단위(건/명), B-26 회복기 비율 불일치, B-27~31 데이터·매뉴얼 항목은 미수정.
- `.claude/agents/*.md` 4개가 작업 중 워킹트리에서 바뀌어 있음 — 이 작업이 건드린 파일 아님(diff에 섞여 나옴).

---

## ③ 화면  — `design`

> 아래 세 줄은 2026-09-21 수정분. 점검 당시 원문(A~D)은 그 아래에 그대로 둔다.

**바꾼 셀렉터**   :
- `style.css` — `.nowrap`(신설·전역) · `.filter-bar .date-range`·`.filter-bar .dr-sep`(신설) · `main.container{padding-top:26px;padding-bottom:34px}`(신설) ·
  `.page-header .btn, .dash-head .btn`(36px 규격, 신설) · `.topbar .global-search form`(38→36) · `.topbar .header-tool-btn`(36×36) · `.topbar .header-new-consult`(height 36) ·
  `.topbar .command-status > span, a`(inline-flex 36, 신설) · `.form-draft-state`(sticky→static, 하나로)
- `dashboard.css` — `.kc`(align-content:start) · `.dash-head`(align-items center) · `.dash-head h1`(1.48em/820) · `.page-dashboard main`(padding-top 14 제거) ·
  `.aq-table`+`col.c-kind/c-who/c-dx/c-meta/c-act`(fixed 68/72/66/100/118) · `.aq-table th,td`(ellipsis) · `.tbl-wrap:has(> .aq-table){overflow:visible}` · `.aq-detail`(max-width 제거) ·
  `.dash-adm-tbl col.c-when/c-onset/c-room/c-hosp/c-sev`(≤1500 폭 재배분)
- `partners.css` — `.coop-perf-cell`(keep-all) · `.coop-perf-cell .coop-neglect-tag`(block) · `@media(max-width:1500px) .coop-partner-table`(min-width 0, th 1/2/4 16/11/14%)
- 템플릿 인라인 — consult_list.html `.tbl-compact`(min-width 1300, ≤1500 열 폭 11개, sortable ⇅ hover, `td.onset-date-cell` nowrap, `.admexp-row > span` nowrap, `body .container` 좌우만) ·
  stats_hospitals.html `@media(max-width:1500px)` 블록(신설), `td.num` nowrap, `.hospital-page-head h1` 크기 제거 · report_weekly.html `@media(max-width:1500px)` 블록(신설), `thead th{word-break:keep-all}` ·
  stats.html `.page-header h1` 1.2em 제거, `.kpi-row .stat-card`(flex-start) · ward.html `.away-badge`(gap 0), `.away-badge b`(margin-left) · consult_form.html `.form-draft-state` 정의 제거
**걸리는 템플릿** : (grep)
- `.nowrap` 11개: ward · _ward_roster · consult_list · inquiries · sms · consult_detail · documents · _period_calc_body · dashboard · audit · stats_staff — 전부 '한 줄 유지' 의도, 회귀 없음(11·13 재원 화면 재촬영 확인)
- `.filter-bar .date-range` 2개: consult_list · audit
- `main.container` 1개: base.html(전 화면) — partner_embed.html은 `<main>`(`.coop-embedded main` 별도)
- `.page-header .btn` 24화면 전부(h1probe로 36px 확인) · `.dash-head .btn` dashboard만
- `.kc` dashboard만 · `.aq-table` dashboard만 · `.dash-adm-tbl` dashboard만 · `.coop-partner-table` partners만 · `.hospital-table` stats_hospitals만 · `.weekly-report-table` report_weekly만 · `.tbl-compact` consult_list만(dashboard의 2곳은 앞 작업에서 제거됨)
- `.form-draft-state` consult_form(form.js가 생성) · `.away-badge` ward(_away_badges include: ward·ward_away)
- 필터 `krdate_short`/`krdate_short_wd`/`krdate_wd` 16개 템플릿(_ward_bed·_ward_roster·_patient_todo_card·consult_detail·dashboard·consult_list·_todos_body·ward 등) · `pct1` 7개 템플릿 27곳 · `room_no` 6개 · `comma` 3개
**확인한 폭**     : 1440×850 (사이드바 펼침 카드 1110~1154 / 접힘 1346 둘 다), 크로미움 headless, device_scale_factor 1. 가로 스크롤 0, 카드 안 내부 스크롤로 열이 숨는 표 0(상담목록·모병원·주간·기관협력·오늘 처리 필요 전부 카드 폭 = 표 폭).

**스크린샷(after)** — `scratchpad/shots/`: after-aq-open(더보기 메뉴) · zoom-after-aq-0 · after-04-consultations · zoom-after-cl-row3-0 · after-20-stats-hospitals-s1 · zoom-after-weekly-0 · zoom-after-partners-0 · after-24-partners-s0 · after-03-consult-new-s0(제목·버튼·임시저장 칩) · after-01-dashboard-s0 · after-kpi-dash/-ward/-stats · zoom-after-adm-0(입퇴원 표 `09-21(월)`·`1009호`) · after-29-admin-audit · after-13-ward-moves · after-11-ward-waiting-full · after-06-consult-detail. 전부 Read로 열어 확인.

**KPI 카드 점검 표** (1440, 수정 후)

| 항목 | 대시보드 `.kc` 8장 | 재원 관리 `.wd-k` 6장 | 재원 추이 `.wd-k` 10장 | 통계 `.stat-card` 6장 | 판단 |
|---|---|---|---|---|---|
| 카드 높이 | 1행 139 / 2행 123 (행 안 동일) | 84 | 68 / 87(두 그룹) | 112 | 한 화면 안 같은 줄은 동일 — OK |
| 패딩 | 10 16 9 | 10 12 | 10 12 | 14 12 10(수정) | 스킨 차이, 보고만 |
| 숫자 크기 | 28px | 25.6px | 25.6px | 22.4px | 화면 간 차이 — dashboard.css 전용 스킨, 보고만 |
| 단위 위치·크기 | 숫자 옆 14.4px | 숫자 옆 12.8px(총 입원 환자·외진은 단위 없음) | 12.8px | 10.75px(em) | 재원 1·5번 카드만 단위 없음 — 보고 |
| 증감 배지 | ▲▼ 12.16px, top 74 전부 동일(수정 전 74/82 혼재) | 없음 | 없음 | 전월/전년 2줄 9.92px | OK |
| 보조줄 `…` 잘림 | 0 (수정 전 3장) | 0 (수정 전 2장) | 0 (수정 전 2장) | 0 | 해결 |
| 스파크라인 | 96×38 전부 | 없음 | 없음 | 없음 | OK |
| 아이콘 | 30×30 전부 | 이모지 텍스트 | 없음 | 없음 | 스킨 차이 |
| 숫자 기준선 | 동일 | 동일 | 동일 | 동일(수정 전 1~3 vs 4~6 15px 어긋남) | 해결 |
| 퍼센트 자리수 | 41.4 / 76.9 / 14.3 (1자리 통일) | 41.8 등 1자리 | 1자리 | 14.3 | 해결 |

---

(아래는 2026-09-21 00:50 점검 당시 원문)

**바꾼 셀렉터**   : 없음 — 점검·보고만. 파일 수정 0건.
**걸리는 템플릿** : (수정 없음) 다만 아래 B-2·B-9 참고 — `.nowrap` 미정의 8개 템플릿, `.filter-bar .date-range` 미정의 audit.html
**확인한 폭**     : 1440×850 (device_scale_factor 1, 크로미움 headless)

스크린샷: `/tmp/claude-1000/-home-sjh35-Developer-bokju-crm/fec102ec-e0fe-4077-a341-5e9071fc084d/scratchpad/shots/`
(`NN-경로.png` 뷰포트, `NN-경로-full.png` 전체, `zoom-*`·`crop-*` 확대, `03-consult-new-s0~6.png` 스크롤 분할, `_report.json` DOM 치수)

### A. 화면별 상태 표

| # | 경로 | 가로스크롤(문서) | 발견 | 스크린샷 |
|---|---|---|---|---|
| 01 | `/` 대시보드 | false | 6 | 01-dashboard(-full), zoom-dash-0/2 |
| 02 | `/calendar` | false | 0 | 02-calendar |
| 03 | `/consult/new` | false | 4 | 03-consult-new-s0~s6 |
| 04 | `/consultations` | false | 3 | 04-consultations, zoom-cl-0 |
| 05 | `/consultations/inquiries` | false | 2 | 05-inquiries(-full) |
| 06 | `/consult/8070` | false | 3 | 06-consult-detail(-full) |
| 07 | `/consult/8070/edit` | false | (03과 동일) | 07-consult-edit |
| 08 | `/patients/7830` | false | 2 | 08-patient-detail-full |
| 09 | `/documents` | false | 1 | 09-documents |
| 10 | `/ward` | false | 2 | 10-ward |
| 11 | `/ward?tab=waiting` | false | 3 | 11-ward-waiting-full |
| 12 | `/ward?tab=away` | false | 1 | 12-ward-away |
| 13 | `/ward?tab=moves` | false | 3 | 13-ward-moves |
| 14 | `/ward?tab=trend` | false | 2 | 14-ward-trend-full |
| 15 | `/ward?tab=blacklist` | false | 0 | 15-ward-blacklist |
| 16 | `/ward?tab=quality` | false | 1 | 16-ward-quality |
| 17 | `/sms` | false | 1 | 17-sms-full |
| 18 | `/sms/templates` | false | 1 | 18-sms-templates |
| 19 | `/stats` | false | 2 | 19-stats |
| 20 | `/stats/hospitals` | false | 3 | 20-stats-hospitals |
| 21 | `/stats/staff` | false | 1 | 21-stats-staff-full |
| 22 | `/report/weekly` | false | 2 | 22-report-weekly-full |
| 23 | `/report/monthly` | false | 1(CDN) | 23-report-monthly-full |
| 24 | `/partners` | false | 3 | 24-partners(-full) |
| 25 | `/notices` | false | 1 | 25-notices |
| 26 | `/support/` | false | 1 | 26-support, crop-title-26-support |
| 27 | `/todos` | false | 1 | 27-todos |
| 28 | `/admin/users` | false | 2 | 28-admin-users |
| 29 | `/admin/audit` | false | 2 | 29-admin-audit |
| 30 | `/admin/import` | false | 1 | 30-admin-import, crop-title-30-admin-import |
| 31 | `/help` | false | 1 | 31-help |

404·권한 오류 없음(전부 200). **문서 레벨 가로 스크롤은 0건**이지만, 카드 안 `.tbl-wrap{overflow-x:auto}`(style.css:210)
**내부 스크롤로 열이 숨는 표가 5곳**(B-1·3·4·5·6) — 화면에는 잘린 것으로 보인다.

### B. 결함 목록 (심각도순)

**[잘림·넘침]**

1. **대시보드 '오늘 처리 필요' — 사용자가 든 예. 미처리(문의) 행 136px vs 나머지 행 49px, 그리고 '액션' 열이 통째로 숨음.**
   위치: 01-dashboard-full 하단 좌측, zoom-dash-0. `templates/dashboard.html:329-356` (`action_tr`), `:360-368` (`action_table`).
   측정: 표 폭 690px > `.tbl-wrap` 534px(좌 7/12 칼럼) → 156px 내부 스크롤, '경과' 열 절반·'액션' 열 전부가 화면 밖.
   액션 셀은 100px에 버튼 4개(✎ 답변·상담 등록·부재중·완료)가 세로로 4줄 쌓여 행이 136px. 운행 행은 버튼 1개라 49px.
   원인: ① `templates/dashboard.html:886` `.dash-action-cell{white-space:normal}`이 같은 td의 `nowrap`을 무효화(같은 특이성, 뒤에 선언)
   → 버튼이 줄바꿈. ② `dashboard.css:476` `.aq-detail{max-width:240px; nowrap}` + 경과 셀 `nowrap`으로 내용·경과 열이 폭을 고정 점유,
   남는 폭이 없어 표가 카드보다 넓어짐(`table-layout:auto`). ③ `dashboard.css:111` `.dash-cols{7fr 5fr}` — 1440에서 좌 칼럼 실폭 570px.
   제안: 문의 행 액션을 '답변' 1개 + `…` 더보기(또는 select)로 줄이고 `.aq-detail max-width`를 `min-width:0` 비율 배분으로 바꾸거나
   1440 이하에서 `.dash-cols`를 1열로.

2. **`.nowrap` 클래스가 style.css에 없다** — dashboard.html:790·audit.html:187·stats_staff.html:273 안에서만 정의.
   `class="nowrap"`을 쓰는데 정의가 없는 템플릿 8개: `ward.html`(37곳) `_ward_roster.html`(10) `consult_list.html`(16)
   `inquiries.html`(9) `sms.html`(7) `consult_detail.html`(6) `documents.html`(5) `_period_calc_body.html`(4).
   CLAUDE.md가 경고한 바로 그 사고. B-8(입원 대기 환자명 줄바꿈)이 이 결과다.
   제안: `.nowrap{white-space:nowrap}` 한 줄을 style.css 전역으로 올리고 세 템플릿의 중복 정의 제거.
   (걸리는 템플릿 11개 — 위 8 + 정의 있는 3. 효과는 '한 줄 유지' 방향이라 회귀는 낮지만 ward.html 37곳은 폭 재확인 필요.)

3. **상담목록 표 — 열 머리글 잘림 + 마지막 열 숨음.** 04-consultations, zoom-cl-0.
   `.tbl-compact{min-width:1380px}`(consult_list.html:472)인데 사이드바 접힘 상태 `.tbl-wrap`은 1346px → 34px 내부 스크롤,
   20열 '상담자' 잘림. 바로 위 주석(:470)은 "최소폭 1300px: 1440 화면엔 가로 스크롤 없이"라고 적혀 있어 **주석과 값이 다르다.**
   ≤1500px 규칙(:525~)에서 성별·나이 27px → 머리글 "성…" "나…", 상담방법 36px → "상담방…"(sw 65), 상담자 44px → 'admin(비상)' 잘림.
   제안: min-width 1300으로 되돌리거나(주석대로) 성별·나이 머리글을 '성'·'나이' 대신 폭 32px 이상.

4. **모병원 분석 표(`/stats/hospitals`) — 표 1460px / 카드 1154px → 306px 숨음.** 20-stats-hospitals.
   '기관연계·직접 문의' 열 그룹이 화면 밖. '환자' 열 "16 명"이 두 줄(단위 앞 공백에서 꺾임). `templates/stats_hospitals.html` `.hospital-table`.
   제안: 열 그룹을 접거나(전기 대비는 툴팁) 1440 이하 폰트·패딩 축소 규칙 추가, "16명"으로 붙여 쓰기.

5. **주간 현황 표(`/report/weekly`) — 표 1500px / 카드 1090px → 410px 숨음.** 22-report-weekly-full.
   '지역·상담유형' 그룹이 안 보임. 머리글 "검색(블로그)" 두 줄. `templates/report_weekly.html` `.weekly-report-table`.
   PNG 저장용 표라 폭을 줄이기 어려우면 화면에서는 `transform:scale` 축소 미리보기가 대안.

6. **기관협력 협력기관 표 — 1180px / 1110px → 70px 숨음**(24-partners-full 맨 아래, 마지막 열 "다…"). 같은 표 '최근 3개월 실적' 셀
   "상담 2 · 입원 0"이 "입/원 0"으로 글자 중간에서 꺾임. `templates/partners.html` `.coop-partner-table`.

7. **대시보드 KPI 보조줄 3장 `…` 잘림**(경계, 폰트 따라 ±). zoom-dash-2. `dashboard.css:96` `.kc .s{nowrap; ellipsis}`.
   "지난달 동기간 32건 · 28.3% · 예정 3 · 보…"(sw 258/cw 246), "빈 병상 82 · 허가 355 · 2병동 포화 · 10…"(277/246),
   "회복기 113/273 · 7일 후 38.95% · 여유…"(255/246). 재원 관리 KPI 3곳도 같은 패턴(10-ward 3·6번 카드, 14-ward-trend 2·6번 카드).
   제안: 보조줄을 두 줄 허용(`white-space:normal; line-clamp:2`)하거나 항목 수 줄이기.

8. **재원 관리 > 입원 대기 표 — 환자명·나이가 글자 중간에서 꺾임.** 11-ward-waiting-full 4번 행("이름 94 / 세·남"), 7번 행("비회복기 대/상"),
   전 행 "상담 2026-08-/04". 환자 열이 좁고 `nowrap` 미정의(B-2). `templates/ward.html` 대기 표.

9. **이력 관리 필터의 기간 입력 2개가 세로로 쌓임.** 29-admin-audit. `templates/audit.html:23-27` `.date-range` 마크업은 consult_list와 같은데
   `.filter-bar .date-range{display:flex}`가 **consult_list.html:769 안에만** 있다(style.css엔 `.period-inline-filter .date-range`뿐, :672).
   B-2와 같은 종류의 사고. 제안: `.filter-bar .date-range`를 style.css로.

10. **대시보드 입·퇴원 현황 표 — 일시 "09.21(…", 병실 "100…" 잘림.** 01-dashboard-full 중앙.
    `dashboard.css:401` `col.c-when{74px}`, `:408` `col.c-room{54px}`(≤1500 규칙). '09.21(월)'·'1009호'가 안 들어간다.
    제안: c-when 84 유지·c-room 60 유지하고 대신 유입경로(c-ref) 폭을 줄이기.

11. **상담일지 폼 라벨 두 줄** — "요양병원 본인부담상한제 (1096만원/년)"이 "(1096만원/ 년)"으로 꺾임(03-consult-new-s4).
    폼 UX 원칙(라벨 nowrap) 위반. 상담 상세 "기타 (ARRANGE)" 라벨도 두 줄(06-consult-detail-full, 라벨 열 ~110px).

12. **상담일지 폼 `임시저장 준비` 칩이 입력칸을 덮는다.** 03-consult-new-s1~s5 우상단. `style.css:598` **와 :1685에 `.form-draft-state`가
    두 번 정의**(top 67/70px, z-index 18/8 서로 다름), `position:sticky`라 스크롤 내내 '연락처'·'기타 메모'·'마비 상세' 칸 위에 얹힌다.
    제안: 정의 하나로 합치고 폼 안 흐름(sticky 해제)으로.

**[높이·정렬]**

13. **페이지 제목이 상단바에 붙는다(여백 0) + 화면마다 제목 위치·크기 제각각.** `<main class="container">`(base.html:216)에
    `.container{padding:0 20px}`(style.css:38)가 `main{padding:26px 0 34px}`(:136)를 덮어 **main 상단 패딩이 0**.
    h1 top: 대부분 59px(=상단바 하단), 대시보드 83, 팩스·문서/모병원/직원소개 84, 통계 65, 재원 61.
    h1 크기: 23.68px 기본, 대시보드 20.8, 통계 19.2, 모병원 25.6. `/todos`·`/admin/*` 4장은 h1이 없고 **h2(보통 굵기)**.
    제안: `main.container{padding-top:26px}` 한 줄 + `.page-header h1` 통일, admin/todos도 `.page-header` 구조로.

14. **상단 헤더 버튼 높이 6종** — 29px(상담일지 폼·월간보고서), 31(상세·문자·매뉴얼 등), 37(공지 작성), 40(팩스·문서, 통합 달력),
    41(상담목록·재원 새 상담일지), 42(통계·대시보드 새 상담일지). 같은 '＋ 새 상담일지'가 41과 42로 다르고 글자 13.6/14.4px.
    대시보드 `.dash-head-actions` 안에서도 통합 달력 40 vs 새 상담일지 42(`_report.json` 01 sibDiff).
    제안: `.page-header .btn`을 한 규격(예 36px/.9rem)으로.

15. **상단바 3요소 높이 36/38/33px**(command-status/global-search/header-tools, `style.css:1340` `.header-inner`) — 세로 중앙이라 눈에 덜 띄지만
    검색창만 2px 크다. (사용자가 말한 '미처리 1' 자체는 입원·퇴원과 같은 36px — 상단바는 정상.)

16. **채널 문의 내역 문의 목록 — 미처리 행만 액션 버튼 3개가 두 줄로 쌓여 행이 높다**(05-inquiries-full 1번 행). B-1과 같은 구조.

17. **통계 대시보드 — 같은 행 카드 높이 330 vs 270**('월별 상담·입원 추이' vs '연도별', 19-stats). 핵심 성과 6장은 1~3번(증감 2줄)과
    4~6번(증감 없음) 숫자 기준선이 15px 어긋남.

18. **사용자 관리 권한 카드 7+1** — `.perm-grid{auto-fill minmax(150px)}`(style.css:883)에 메뉴 8개라 1440에서 '사용자 관리' 1장만 둘째 줄.
    새 계정 추가 행 input 36 / select 38 / 버튼 38.

19. **상담일지 폼 입력 높이 혼재** — 같은 행에 date 37px / time 24px / select 34px / 콤보 35px(상단 입원예정 행), 병명 상세 input 23px
    vs 암 상세 28px vs 기타 텍스트 60px. `_report.json` 03 byH: 23px×32, 35px×15, 37px×7, 28px×5.

20. **기관협력 — 페이지 진입 시 871px 스크롤된 채 열림.** `partners.html:13` 검색 input `autofocus` → 제목·'오늘의 기관협력 업무'가 안 보인다.
    제안: autofocus 제거.

**[형식 혼용]**

21. **빈값 표기 4종 혼용** — `-`(상담 상세·입퇴원 표), `—`(입원·퇴원 이력 퇴원일·직원소개 기관/부서), `(미기재)`(재연락 표), 공란(환자 상세 상담 이력).
    같은 표 안에서도 주치의 `-` / 퇴원일 `—`(13-ward-moves).

22. **보호자 표기** "- (아들) 010-…" — 이름 없을 때 대시(-)가 앞에 남는다(06·08·11번 화면). 상담목록은 "아들 010-…"(관계 배지).

23. **호실 표기** 같은 열에 "317호 ★" / "505" / "1005호"(13-ward-moves) — 명부 값 그대로라 '호' 유무 혼재. `room_no` 필터를 일괄 적용 제안.

24. **비율 소수 자리** 41.39%(대시보드) / 76.9% / 14.3% / 0.00%(직원소개 회복기) / 40.0% — 2·1·2자리 혼재.
    **천 단위 콤마** "총 8069건"(상담목록) "41815곳"(기관협력) vs "3,000"(통계 축).

25. **단위** 입원을 대시보드는 "14건", 모병원 분석은 "10명", 입원 이력은 "65명"; 배지 "복귀 18 명"(공백)과 카드 "18명".

**[문구]** → C 표.

**[데이터·기타 — 보고만]**

26. **회복기 비율이 화면마다 다르다**: 대시보드 KPI 41.39%(113/273), 재원 현황 KPI 41.76%(114명), 회복기 추이 48.13%(90/187 판정 가능).
    분모·분자 셋 다 다름. CLAUDE.md '숫자가 화면마다 다르면 아무도 안 믿는다'. 백엔드 확인 필요.
27. 데이터 품질 점검 표 '환자' 열에 날짜 문자열("26.07.31" 등)이 이름으로 들어간 행 2개(16-ward-quality 좌측 4·9번 행) — 적재 오류.
28. 이력 관리 분류 칩에 라벨 없는 원문 키 6종: `recovery_call` `update_admission_event` `roster_import` `export_xlsx` `transport_save`
    `change_own_password` → `config.AUDIT_ACTION_LABELS` 누락.
29. 매뉴얼 목차에 폐기된 '인박스' 링크(31-help). 엑셀 적재 화면에 서버 절대경로 노출("저장 위치: /home/…").
30. `report_monthly.html:4` Chart.js를 jsdelivr CDN에서 받는다 — 사내망 CDN 차단 시 월간보고서 차트 4개가 빈칸(이번 캡처와 동일). 폰트와 같은 위험.
31. 데이터·백업 점검 탭만 활성일 때 건수(374)가 붙고 비활성일 땐 안 붙는다(입원 대기 9·블랙리스트 0은 항상 표시).

### C. 화면 간 불일치 표 (문구·형식)

| 항목 | 화면 A | 화면 B | 통일 제안 |
|---|---|---|---|
| 상담 담당자 호칭 | 상담목록·폼·상세·인쇄 "상담자"(14곳) | 대시보드 KPI "상담사 0명"·통계·공지·사용자 관리 "상담사"(22곳); 대시보드는 한 화면에 둘 다(3/5) | 화면 라벨은 "상담자"(양식 용어), 직군 지칭만 "상담사" |
| 유입 경로 | 상담 상세·인쇄·폼 fieldset "입원경로" | 상담목록·대시보드·통계·보고서 "유입경로" | "유입경로"(CLAUDE.md 데이터 모델 명칭) |
| 입원예정 띄어쓰기 | 상담 상세·달력·대시보드 "입원예정" | 재원 관리 "입원 예정"(22곳), 대시보드 KPI 보조줄 "입원 예정" | 상태값은 "입원예정", 문장 속만 띄움 |
| 처리 완료 | 문의 내역 배지 "처리완료" | 대시보드 버튼 "완료"·툴팁 "처리 완료" | "처리완료" |
| 할 일 | 사이드바 "To-DO List" | 페이지 "내 할 일", 대시보드 "오늘 할 일", 달력 "나의 ToDo", 버튼 "새 ToDo" | "할 일"(한글) 또는 "To-Do" 한 가지 |
| 새 상담 | 상단바 "＋ 새 상담", 사이드바 "새 상담" | 페이지·버튼 "새 상담일지" | "새 상담일지" |
| 상담 목록 | 사이드바 "상담목록" | 페이지 제목·매뉴얼 "상담 목록" | 하나로 |
| 재원 메뉴 | 사이드바 하위 "재원 현황" | 페이지 제목 "재원 관리" | — |
| 날짜(연월일) | 상단바 `2026.09.21 (월)` (app.py:540, 괄호 앞 공백) | 대시보드 `2026.09.20(일)`(main.py:381) / `2026-09-19(토)` / 목록 `26.09.19(토)` / 입퇴원표 `09.21(월)` / 기한임박 `9/21(월)` / 주간표 `09/21(월)` / KPI `09.01~09.21` | 필터 5종(app.py:644~) + 수기 3종 = 8가지. 목록형은 `26.09.19(토)`, 문장형은 `2026-09-19(토)` 둘로 |
| 날짜 입력칸 표시 | — | `mm/dd/yyyy`는 브라우저 로케일(캡처 환경 en-US). 사용자 한국어 크롬에선 `연도. 월. 일.` | 결함 아님(참고) |
| 빈값 | `-` | `—` / `(미기재)` / 공란 | `-` |
| 퍼센트 자리수 | 41.39% / 0.00% | 76.9% / 14.3% | 1자리 |
| 큰 수 콤마 | 8069건 / 41815곳 | 3,000 | 콤마 |
| 입원 단위 | 건 | 명 | 명(사람) |
| 페이지 제목 태그 | h1(굵게) | h2(보통) — todos·users·audit·admin_import | h1 + `.page-header` |

### D. 총평

1. **먼저 고칠 것 ①** — 대시보드 '오늘 처리 필요' 표(B-1): 미처리 문의 행 버튼 4개가 세로로 쌓이고 액션 열이 통째로 숨는다. 사용자가 본 그 문제.
2. **②** — `.nowrap`·`.filter-bar .date-range`를 style.css 전역으로(B-2·B-9): 정의 없는 클래스가 8+1 템플릿에서 헛돌고 있고 B-8·이력 관리 필터가 그 결과.
3. **③** — 1440에서 카드 밖으로 넘치는 표 4개(상담목록 34px·모병원 306px·주간 현황 410px·기관협력 70px, B-3~6) + `main.container` 상단 패딩 0(B-13).
4. 형식은 날짜 8종·빈값 4종·퍼센트 자리수가 섞여 있다 — 필터 하나로 몰아 쓰는 규칙만 정하면 된다.
5. 회복기 비율이 화면 3곳에서 41.39/41.76/48.13%로 다르다(B-26) — 디자인 밖이지만 가장 먼저 사람 눈에 띌 것.

---

## ④ 확인한 것  — `verify`

해당없음 (수정 없음)

---

## ⑤ 내보냄  — `ship`

해당없음
